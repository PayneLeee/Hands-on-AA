# 环境配置完整指南

本文档提供项目环境配置的完整指引，适用于本地环境、云端环境等各种场景。

## 📋 目录

- [环境要求](#环境要求)
- [快速开始](#快速开始)
- [详细配置步骤](#详细配置步骤)
- [IDE配置](#ide配置)
- [GPU配置（可选）](#gpu配置可选)
- [API配置](#api配置)
- [验证安装](#验证安装)
- [常见问题](#常见问题)
- [云端环境配置](#云端环境配置)

---

## 环境要求

### 基础要求

- **Python**: 3.10 或更高版本
- **内存**: 8GB+ RAM (推荐16GB+)
- **磁盘空间**: 10GB+ (用于模型下载和依赖安装)

### GPU要求（可选）

- **GPU**: NVIDIA GPU (如果使用GPU加速)
- **CUDA**: 11.8+ (如果使用GPU)
- **驱动**: 最新的NVIDIA驱动

---

## 快速开始

### 方法1: 使用 Conda（推荐）

```bash
# 1. 创建环境
conda create -n hands-on-aai python=3.10 -y

# 2. 激活环境
conda activate hands-on-aai

# 3. 安装PyTorch（根据您的需求选择）
# CPU版本
pip install torch torchvision torchaudio

# 或CUDA版本（根据您的CUDA版本选择）
# CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# CUDA 12.4: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# 4. 安装其他依赖
pip install -r requirements.txt
```

### 方法2: 使用 venv

```bash
# 1. 创建虚拟环境
python -m venv venv

# 2. 激活虚拟环境
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

# 3. 安装PyTorch和依赖（同上）
pip install torch torchvision torchaudio
pip install -r requirements.txt
```

### 方法3: 使用 environment.yml

```bash
# 从environment.yml创建环境（Conda）
conda env create -f environment.yml
conda activate hands-on-aai
```

---

## 详细配置步骤

### 步骤1: 创建Python环境

选择适合您的方式：

**Conda方式（推荐）:**
```bash
conda create -n hands-on-aai python=3.10 -y
conda activate hands-on-aai
```

**venv方式:**
```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

### 步骤2: 安装PyTorch

根据您的需求选择：

**CPU版本（无GPU或不需要GPU加速）:**
```bash
pip install torch torchvision torchaudio
```

**CUDA版本（需要GPU加速）:**
```bash
# 首先检查CUDA版本
nvidia-smi

# 根据CUDA版本选择对应的PyTorch版本
# CUDA 11.8
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1 (兼容12.1-12.5)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# CUDA 12.4 (兼容12.4+)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
```

详细说明请参考本文档的 [GPU配置（可选）](#gpu配置可选) 部分。

### 步骤3: 安装项目依赖

```bash
pip install -r requirements.txt
```

### 步骤4: 配置API密钥（可选）

部分章节需要使用API密钥，配置方法请参考 [API配置使用指南](./API配置使用指南.md)。

---

## IDE配置

### Cursor / VS Code

#### 自动选择解释器

1. 打开命令面板（`Ctrl+Shift+P` / `Cmd+Shift+P`）
2. 输入 "Python: Select Interpreter"
3. 选择 `hands-on-aai` 环境的Python解释器

#### 手动配置

创建或编辑 `.vscode/settings.json` 或 `.cursor/settings.json`：

**使用conda环境（推荐）:**
```json
{
    "python.defaultInterpreterPath": "${env:CONDA_PREFIX}/python.exe",
    "python.terminal.activateEnvironment": true
}
```

**使用venv:**
```json
{
    "python.defaultInterpreterPath": "./venv/Scripts/python.exe",
    "python.terminal.activateEnvironment": true
}
```

**手动指定路径:**
```json
{
    "python.defaultInterpreterPath": "C:\\Anaconda\\envs\\hands-on-aai\\python.exe",
    "python.terminal.activateEnvironment": true
}
```

详细说明请参考本文档的 [IDE配置](#ide配置) 部分。

---

## GPU配置（可选）

### 检查GPU支持

```bash
# 检查GPU
nvidia-smi

# 检查CUDA版本
nvcc --version
```

### 安装CUDA版本的PyTorch

根据您的CUDA版本选择对应的PyTorch版本，详见本文档的 [GPU配置（可选）](#gpu配置可选) 部分。

### 验证GPU可用

```python
import torch
print(f"CUDA available: {torch.cuda.is_available()}")
if torch.cuda.is_available():
    print(f"GPU: {torch.cuda.get_device_name(0)}")
    print(f"CUDA version: {torch.version.cuda}")
```

---

## API配置

### 方式1: 使用配置文件（推荐）

创建 `HandsOn/config.json`（参考 `HandsOn/config.json.example`）：

```json
{
  "openai": {
    "api_key": "your_api_key_here",
    "base_url": "https://api.openai.com/v1",
    "model": "gpt-4o-mini"
  }
}
```

### 方式2: 使用环境变量

创建 `.env` 文件：

```env
OPENAI_API_KEY=your_api_key_here
DEEPSEEK_API_KEY=your_api_key_here
```

详细说明请参考 [API配置使用指南](./API配置使用指南.md)。

---

## 验证安装

运行以下代码验证环境是否正确配置：

```python
import sys
import torch
import transformers
import langchain

print("=" * 50)
print("环境验证")
print("=" * 50)
print(f"Python版本: {sys.version}")
print(f"Python路径: {sys.executable}")
print(f"PyTorch版本: {torch.__version__}")
print(f"CUDA可用: {torch.cuda.is_available()}")
if torch.cuda.is_available():
    print(f"CUDA版本: {torch.version.cuda}")
    print(f"GPU设备: {torch.cuda.get_device_name(0)}")
print(f"Transformers版本: {transformers.__version__}")
print(f"LangChain版本: {langchain.__version__}")
print("=" * 50)
```

预期输出：
- Python版本为3.10+
- PyTorch版本正确安装
- CUDA可用（如果安装了GPU版本）
- 所有主要依赖包版本正确

---

## 常见问题

### Q1: 如何选择PyTorch版本？

**A:** 
- 如果没有GPU或不需要GPU加速，使用CPU版本
- 如果有GPU，根据CUDA版本选择对应的PyTorch版本
- 参考本文档的 [GPU配置（可选）](#gpu配置可选) 部分中的版本对应表

### Q2: 找不到conda环境？

**A:**
1. 确认环境已创建：`conda env list`
2. 确认环境名称正确：`conda activate hands-on-aai`
3. 如果使用IDE，手动选择解释器路径

### Q3: 模型下载失败？

**A:**
使用Hugging Face镜像：

```python
import os
os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'
```

### Q4: 内存不足？

**A:**
- 使用量化模型
- 减小batch size
- 使用CPU版本的模型
- 使用更小的模型

### Q5: API调用失败？

**A:**
1. 检查API密钥是否正确配置
2. 检查网络连接
3. 检查API额度是否充足
4. 参考 [API配置使用指南](./API配置使用指南.md)

---

## 云端环境配置

### Google Colab

Colab已预装大部分依赖，通常只需要：

```python
# 安装额外依赖
!pip install -r requirements.txt

# 验证GPU
import torch
print(f"CUDA available: {torch.cuda.is_available()}")
```

### Kaggle

```python
# 安装依赖
!pip install -r requirements.txt

# 如果需要特定版本的PyTorch
!pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

### 其他云端平台

1. 检查平台是否预装Python和依赖
2. 根据平台文档安装缺失的依赖
3. 配置API密钥（如果需要）
4. 验证环境配置

---

## 相关文档

- [API配置使用指南](./API配置使用指南.md) - 详细的API配置系统说明（包含设计理念、核心组件、使用方法等）
- [README.md](../../README.md) - 项目总体说明

---

## 总结

环境配置步骤总结：

1. ✅ 创建Python环境（conda或venv）
2. ✅ 安装PyTorch（CPU或CUDA版本）
3. ✅ 安装项目依赖（`pip install -r requirements.txt`）
4. ✅ 配置API密钥（可选）
5. ✅ 配置IDE（可选）
6. ✅ 验证安装

完成以上步骤后，您就可以开始使用本项目了！

---

**最后更新**：2025年1月
